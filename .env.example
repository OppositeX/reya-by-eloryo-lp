# Inquiry form delivery (app/api/inquiry/route.ts).
# With these unset the endpoint still succeeds and logs the lead instead.
RESEND_API_KEY=
INQUIRY_TO=sales@example.com
INQUIRY_FROM="Reya <noreply@yourdomain.com>"
