# dependencies
node_modules

# next.js
.next
out
next-env.d.ts

# env
.env*
!.env.example

# local-only working material
.thumbnail
screenshots/
design_handoff_reya_landing/

# Source images are local-only. Only the optimized .webp files under public/
# are committed -- regenerate them with `npm run optimize`.
public/**/*.png
public/**/*.jpg
public/**/*.jpeg

# vercel / os
.vercel
.DS_Store

# typescript incremental build info
*.tsbuildinfo
